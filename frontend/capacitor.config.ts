import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "my.airintel.malaysia",
  appName: "AirIntel Malaysia",
  webDir: "out", // Next.js static export output — see next.config.js
};

export default config;
